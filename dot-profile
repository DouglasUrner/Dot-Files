# Configuration for Bourne Shell deriviative login shells (e.g., bash, zsh).
#
# Won't be read if a shell specific file also exists (e.g. .bash_profile). This
# file could be used to hold configuration that is not shell dependant and then
# source the shell specific commands based on the shell being run.

# Run shell specific commands
case $0 in
*bash)
  ;;
*zsh)
  ;;
default)
  ;;
esac
