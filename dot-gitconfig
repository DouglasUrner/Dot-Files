[user]
	name = Douglas Urner
	email = dlu@canishe.com

[core]
	excludesfile = ~/.gitignore

[credential]
	helper = osxkeychain

[diff]
	# Git diff will use (i)ndex, (w)ork tree, (c)ommit and (o)bject
	# instead of a/b/c/d as prefixes for patches
	mnemonicprefix = true

[push]
	# Push the current branch back to the matching branch on the
	# central repository.
	default = simple

[color]
	ui = true
